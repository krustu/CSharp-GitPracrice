# CSharp-GitPracrice

# basic git commands
git add .
git commit -m ""
git push
git status 
git pull //update local files


# Terminal commands#
pwd // Check current location
ls 
cd "file name" // forward to
cd .. // get back
mkdir // create new filder
ni // create ne file
rm // deleting

# basic C# commands 
dotnet run
dotnet build // fix and run
dotnet new console m //FileName
dotnet add package Newtonsoft.Json // add dictinary